// Gender.ts
export enum Gender {
  Female = 0,
  Male = 1,
  Other = 2,
}

// Role.ts
export enum Role {
  Admin = 1,
  Manager = 2,
  Customer = 3,
}

// Status.ts
export enum Status {
  Active = 0,
  Inactive = 1,
}